<% if session[:message] %>
  <p class="message"><%= session.delete(:message) %></p>
<% end %>
  <div id="links">
  <a href="/">Sort by 5s</a>
  <a href="/sort_by/4">Sort by 4s</a>
  <a href="/equips/soulcards">Show Soul Cards</a>
  <!-- <a href="/sort_by/tier">Sort by tier</a>
  <a href="/sort_by/type">Sort by type</a>
  <a href="/sort_by/element">Sort by element</a>
  <a href="/sort_by/stars">Sort by stars</a> -->
  <% if session[:username] %>
  <a id="add_new_unit" href="/new_unit">Add new unit</a>
  <a id="add_new_unit" href="/show_unit_details">Get yml details</a>
  <a id="add_new_unit" href="/show_files">Show all image files</a>
  <a id="add_new_unit" href="/upload">Upload images/files</a>
  <% end %>
</div>
<div class="units_list_div">
    <% @units.each do |unit, unit_details| %>
      <% next if unit == "new_unit" %>

        <div class="unit_div <%= unit_details["element"] %>">
          <h2><%= unit.capitalize %></h2>
          <% if session[:username] %>
            <a href="/<%= unit %>/edit">Edit</a>|
            <a href="/<%= unit %>/remove">Remove</a>
          <% else %>
            <p>  </p>
          <% end %>
          <a href="/<%= unit %>"><img class="unit_index_pic" src="<%= unit_details['pic'] %>"/></a>
        </div>
    <% end %>
</div>
